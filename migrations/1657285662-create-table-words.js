exports.up = async (sql) => {
  await sql`CREATE TABLE words (
    id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
		word varchar(50)
  )`;
};
exports.down = async (sql) => {
  await sql`
	DROP TABLE words `;
};
